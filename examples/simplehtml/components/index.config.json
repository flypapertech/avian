{

    "cronJobs": [
        
        {
            "enabled": true,
            "name": "Touch a file locally again.",
            "description": "Will deploy the latest version of Protest to AWS",
            "expression": "* * * * *",
            "command": "touch",
            "args": ["myfile.txt"]
        }
    ]
}
